export PGPASSWORD=$(aws rds generate-db-auth-token \
    --hostname <aurora host> \
    --port 5432 \
    --username postgres \
    --region us-east-1)

 	  
	 or
	 
	 
cat > ~/.pgpass << 'EOF'
<aurora host>:5432:<db_name>:postgres:<pass>
EOF